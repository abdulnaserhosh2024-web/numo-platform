
import 'package:flutter/material.dart';

class GoalWelcomePage extends StatelessWidget {
  const GoalWelcomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Goal Welcome'),
      ),
    );
  }
}
